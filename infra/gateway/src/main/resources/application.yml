spring:
  application:
    name: gateway
  config:
    import: "configserver:"
  cloud:
    config:
      fail-fast: true          # не стартовать без конфига
      retry:
        useRandomPolicy: true
        max-interval: 6000     # макс. 6 сек между попытками
      discovery:
        enabled: true
        serviceId: config-server

eureka:
  client:
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname:localhost}:${eureka.instance.port:8761}/eureka/
  instance:
    preferIpAddress: true
    hostname: localhost
    # уникальный instance-id при нескольких экземплярах Gateway
    instance-id: "${spring.application.name}:${random.value}"
    leaseRenewalIntervalInSeconds: 10